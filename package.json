{
    "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "client": "cd ./client && npm run dev",
    "dev": "concurrently -n 'server,client' \"cd ./server/ && nodemon server.js\" \"npm run client \""
  },
  "dependencies": {
    "concurrently": "^7.3.0"
  }
}